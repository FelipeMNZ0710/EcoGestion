
import type { NewsArticle } from '../types';

// This data is now fetched from the database via the backend server.
// The file is kept to avoid breaking imports, but the data is no longer used.
export const initialNews: NewsArticle[] = [];
